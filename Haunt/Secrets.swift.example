import Foundation
// Copy this to Secrets.swift (gitignored) and fill in the values.
// The fal key is NOT here — it lives on the Railway proxy. This is just the proxy gate.
enum Secrets {
    static let proxyURL = "https://haunt-proxy-production.up.railway.app/summon"
    static let appSecret = "PROXY_APP_SECRET"
}
